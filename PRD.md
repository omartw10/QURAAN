---

# PRD — Quraan

## 1. وصف المشروع (Project Description)

مشروع **Quraan** هو منصة إسلامية ويب متكاملة ومحسنة، تهدف إلى تقديم تجربة مستخدم راقية وبسيطة (Minimalist) للاستماع إلى القرآن الكريم بجودة عالية ومتابعة البث المباشر للحرمين الشريفين (المكي والمدني). ينتقل المشروع من هيكلية الصفحة الواحدة التقليدية إلى موقع متعدد الصفحات (Multi-Page Website) مبني بالكامل باستخدام تقنيات الويب الأساسية دون أي إطارات عمل، مع التركيز على هندسة الكود وجودة التصميم البصري المناسب للسياق الديني.

## 2. الوضع الحالي للمشروع (Current State)

* **الهيكلية الموجودة قبل التطوير:**
* ملف واحد `index.html` يتضمن الهيكل والمحتوى، بالإضافة إلى كود JavaScript مدمج داخلياً.
* ملف تنسيق واحد `Style.css` مبني بتنسيقات عشوائية غير منظمة، ومعتمد على الظلال الضخمة والألوان المتعددة (الأخضر، الكحلي، الأحمر) وبدون استخدام متغيرات CSS.
* مشغل الصوت والوقت يعتمدان على بيانات وهمية وثابتة (Mock Data) وتعمل عبر مؤقت `setInterval` زائف.
* مشغل البث المباشر يعتمد على كود يوتيوب مدمج يدوياً ويظهر خطأ في التهيئة (Error 153).


* **ما سيُحتفظ به:**
* النصوص الأساسية، أسماء القراء والروايات، والسور كبيانات مرجعية.
* فكرة التبديل بين البث المباشر للمسجد الحرام والمسجد النبوي.


* **ما سيُحذف أو يُعاد بناؤه:**
* حذف كود JavaScript المدمج داخل ملف الـ HTML تماماً وفصله إلى ملفات منظمة.
* إلغاء ملف `Style.css` القديم وإعادة بنائه بالكامل باستخدام منهجية BEM ومتغيرات CSS الصارمة.
* التخلص من القوائم المنسدلة التقليدية الخاصة بالمتصفح، وتصميم قوائم مخصصة (Custom UI Components).
* استبدال كود مشغل الصوت المزيف بـ Fetch API حقيقي.



## 3. الهدف والجمهور المستهدف (Goals & Target Audience)

* **الهدف:** تقديم مشروع جامعي نموذجي لمادة (CS351) يثبت قدرة الطالب على هندسة الكود، وفصل المهام (Separation of Concerns)، وبناء واجهة مستخدم احترافية متجاوبة وخالية من العيوب البصرية.
* **الجمهور المستهدف:** المسلمون من الناطقين باللغتين العربية والإنجليزية الراغبون في استماع القرآن والبث المباشر عبر واجهة هادئة ومريحة للعين.

## 4. الصفحات والمحتوى النهائي (Final Pages & Content)

المشروع سيعتمد على بنية متعددة الصفحات (Multi-Page) مع مشاركة الـ Header والـ Footer عبر تضمين يدوي (Manual Include via JS Fetch أوโครงสร้าง HTML ثابت):

1. **الصفحة الرئيسية والمشغل (`index.html`):** تحتوي على قسم الـ Hero الترحيبي، يليه مباشرة مشغل القرآن الكريم الديناميكي.
2. **صفحة البث المباشر (`live.html`):** صفحة مستقلة مخصصة لعرض البث الحي للحرم المكي والحرم المدني مع أزرار تبديل سلسة ومشغل فيديو مستقر.
3. **صفحة تواصل معنا (`contact.html`):** تحتوي على نموذج التواصل (Contact Form) مع تفعيل نظام التحقق من المدخلات.

## 5. التصميم والهوية البصرية (Design & Visual Identity)

* **نمط التصميم:** Minimalist — مساحات بيضاء واسعة، اتجاه RTL إلزامي للمحتوى العربي، تباين عالٍ للنصوص ($>= 4.5:1$)، وحواف مستقيمة حادة بدون ظلال ضخمة غير واقعية.
* **باليتة الألوان الإلزامية (CSS Variables):**
```css
:root {
  --color-primary: #382100;    /* بني داكن كلاسيكي */
  --color-secondary: #000000;  /* أسود نقي */
  --color-accent: #945600;     /* ذهبي فاخر */
  --color-aux-1: #ffffff;      /* أبيض نقي للمساحات والخلفيات */
}

```


* **الخطوط (Typography):**
* للمحتوى العربي: خط **Amiri** للعناوين الكبيرة والطابع الكلاسيكي، وخط **Tajawal** أو **Cairo** للنصوص العادية والقوائم لسهولة القراءة.
* للمحتوى الإنجليزي: خط نظام نظيف (مثل **Inter** أو **system-ui**).


* **العناصر المرئية:** الاعتماد حصرياً على الأشكال الحسابية (CSS Shapes)، التدرجات الناعمة (Gradients)، وأيقونات SVG مفتوحة المصدر (أو Lucide/FontAwesome المسموحة).

## 6. التقنيات المستخدمة (Technical Stack)

* **الهيكل والتنسيق:** Vanilla HTML5 + CSS3 (Advanced Layouts: Flexbox & CSS Grid).
* **منهجية الكود:** BEM methodology لترتيب أسماء الكلاسات (`.block__element--modifier`).
* **البرمجة والتفاعل:** Vanilla JavaScript (ES6+) شامل لإدارة الـ DOM، الـ LocalStorage، والـ Fetch API.
* **مصادر البيانات (API External):** التكامل مع REST API عام ومستقر (مثل **MP3Quran API**) لجلب قراءات السور والملفات الصوتية الحقيقية حركياً بناءً على اختيار المستخدم للرواية والقارئ.

## 7. هيكل المجلدات النهائي المقترح (Proposed Directory Structure)

```text
quran-project/
├── index.html          # الصفحة الرئيسية + مشغل القرآن
├── live.html           # صفحة البث المباشر
├── contact.html        # صفحة نموذج التواصل
├── css/
│   ├── main.css        # المتغيرات والتنسيقات العامة والـ Reset
│   ├── header.css      # تنسيق المكون الهيدر (BEM)
│   ├── footer.css      # تنسيق المكون الفوتر (BEM)
│   ├── player.css      # تنسيق بطاقة المشغل والقوائم
│   └── live.css        # تنسيق مشغل البث والبطاقات
├── js/
│   ├── main.js         # الإعدادات العامة وتضمين الهيدر والفوتر والـ Dark Mode
│   ├── player.js       # منطق الـ Fetch والتحكم بالصوتيات والـ LocalStorage
│   └── contact.js      # التحقق من مدخلات نموذج التواصل (Form Validation)
└── assets/
    └── icons/          # ملفات الـ SVG والأيقونات

```

## 8. الميزات المطلوبة وترتيب أولوياتها (Features & Prioritization)

* **أولوية عالية (High Priority):**
* إعادة هيكلة التصميم بالكامل وتحويله إلى النظام اللوني الجديد (Minimalist Brown/Gold).
* ربط المشغل بـ API حقيقي لجلب الصوتيات حركياً بدل المؤقت الزائف.
* بناء نظام التصفح متعدد الصفحات بأسلوب هندسي نظيف.


* **أولوية متوسطة (Medium Priority):**
* **Dark / Light Mode Toggle:** زر التبديل في الـ Header وحفظ الخيار في الـ `localStorage` مع دعم التفضيل التلقائي للمتصفح.
* **Contact Form Validation:** التحقق الفوري في المتصفح (Client-side) وإظهار رسائل خطأ واضحة مستقيمة تحت كل حقل (الاسم، البريد، الرسالة).


* **أولوية منخفضة (Low Priority):**
* **Micro-animations:** تأثيرات حركية خفيفة جداً (Transitions) لا تتعدى 300ms على الأزرار والقوائم باستخدام `transform` و `opacity` فقط، مع احترام رغبة المستخدم في تقليل الحركة `prefers-reduced-motion`.
* **بصمة المشروع (State Persistence):** حفظ آخر سورة وقارئ استمع إليهم المستخدم في الـ `localStorage` لاستعادتهم تلقائياً عند فتح الموقع مجدداً.



## 9. ما هو خارج نطاق المشروع (Out of Scope)

* لا يتم استخدام أي إطارات عمل (React, Vue, Angular, Bootstrap, TailwindCSS).
* لا توجد لوحة تحكم (Dashboard) أو قواعد بيانات خلفية خاصة (No Node.js/PHP/Supabase)؛ الاعتماد بالكامل على الواجهة الأمامية والـ APIs العامة.
* لا يوجد نظام تسجيل دخول أو حسابات للمستخدمين.

## 10. خطة العمل وتسلسل البناء (Action Plan)

1. **المرحلة الأولى:** إعداد هيكل المجلدات الجديد وتوزيع ملفات HTML الثلاثة الخام وتجهيز ملف متغيرات CSS العام.
2. **المرحلة الثانية:** بناء وتصميم الـ Header والـ Footer المشتركين وتطبيق نظام الـ Light/Dark Mode عليهما.
3. **المرحلة الثانية:** هندسة وتصميم صفحة المشغل الرئيسي (`index.html`) وتطبيق كلاسات BEM الصارمة عليها ثم كتابة الـ JavaScript للربط بالـ API الحقيقي.
4. **المرحلة الرابعة:** بناء صفحة البث المباشر وتجهيز روابط تضمين يوتيوب الرسمية المستقرة للتخلص من أخطاء التشغيل.
5. **المرحلة الخامسة:** بناء صفحة التواصل وتفعيل الـ Validation، ثم مراجعة الأداء والتجاوب الكامل مع شاشات الموبايل (Responsive Breakpoints).

---

